PA EQU 0040H   ;00   选中A口
PC EQU 0044H   ;10   选中C口
PCTL EQU 0046H ;11   选中控制寄存器
CODE SEGMENT
    ASSUME CS:CODE
START:
	MOV DX,PCTL
	MOV AL,10010000B;控制字 A输入口，C输出口，普通IO方式0
	OUT DX,AL
CC:
	MOV AL,0
	MOV DX,PA
	IN AL,DX
	NOT AL
	MOV DX,PC
	OUT DX,AL
	JMP CC

CODE ENDS
END START